package f_samp.spring.service;

//import f_samp.database.dao.PersonDAO;
import f_samp.database.model.Persons;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class PersonServiceLMPL implements PersonsService {
/*
    private final static Logger log = Logger.getLogger(PersonServiceLMPL.class);

   //@Autowired
    private  PersonDAO PersonList;

    @Override
    public List<Persons> listAll() {
        return PersonList.findAll();
    }

    @Override
    public void delete(String passport) {
        PersonList.remove(passport);
    }

    @Override
    public Persons add(String name,  String surname, String patronymic, String passport, String position, String tolerance_level) {
        return PersonList.persist(name,  surname,  patronymic, passport,  position,  tolerance_level);
    }


*/


}
